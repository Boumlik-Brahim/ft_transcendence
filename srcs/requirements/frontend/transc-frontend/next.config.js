/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com','cdn.intra.42.fr','images.unsplash.com'],
      },
  }
  
  module.exports = nextConfig
  