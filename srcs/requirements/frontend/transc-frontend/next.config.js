/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com', 'cdn.dribbble.com','localhost','cdn.pixabay.com', 'imgtr.ee', 'cdn.intra.42.fr','t.ly'],
      },
  }
  
  module.exports = nextConfig
  